package com.myfirstmaven.test.HelloMaven;

import org.eclipse.jetty.server.Server;
import org.eclipse.jetty.servlet.ServletContextHandler;
import org.eclipse.jetty.servlet.ServletHolder;
//import com.google.gson.Gson;
//import com.google.gson.GsonBuilder;

public class App {
	public static void main(String[] args) throws Exception {
		//Gson gson = new GsonBuilder().create();
		//gson.toJson("Hello", System.out);
		System.out.println();
		// System.out.println();
		// Person person1 = new Person ("Andrei","andrei@bla.com");
		// gson.toJson(person1,System.out);

		// DataBaseConnection myConnection = new DataBaseConnection();
		// myConnection.WriteToDatabase("faraimport2", "gmai2l");

		/*Server server = new Server(8090);

		ServletContextHandler context = new ServletContextHandler(ServletContextHandler.SESSIONS);
		context.setContextPath("/");
		server.setHandler(context);

		context.addServlet(new ServletHolder(new HelloWorld()), "/hello");

		server.start();
		server.join();*/

	}
}
